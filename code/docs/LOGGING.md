# Logging

## Output dir
/root/capsule/results/results/

## Fields
- metric
- commit hash
- commit message (header)
- commit message (body)
- timestamp
- action (e.g. `evaluated`, `kept`, `discarded`, `crashed`)

## Format
TODO: TSV, CSV, JSONL, SQLite, Markdown table, or another format. 
