#!/usr/bin/env bash
set -ex

export UV_CACHE_DIR=/root/capsule/scratch/.cache
mkdir -p "${UV_CACHE_DIR}"

copilot "$@" --allow-all --no-ask-user --share=/root/capsule/results/copilot-session.md # --silent 