#!/usr/bin/env bash
set -ex

echo $TERM

export UV_CACHE_DIR=/root/capsule/scratch/.cache
mkdir -p "${UV_CACHE_DIR}"

export COPILOT_HOME=/root/capsule/.copilot
copilot --version
copilot "$@" --autopilot --yolo --share=/root/capsule/results/copilot-session.md  # --silent 